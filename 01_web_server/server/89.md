ngnx
web server

// js not capable of listening to http event , this whole functionality of server actually depend on libuv.

// libuv is responsible for binding all that request and giving u  an object of request
// if is writable object u can change the request , u can change the response 

// creating a server 

// find what is the request is coming in 
// what should i do when request comes up 
// for ('/') serve index.html 

//join()  :: has a property to reference to its current directory, where u are 

// path can give some extension sometime write html in upper or lowercase 
   //extname == returns extension of the path from the last "." to end of the string 

    // have to define object content type 
  // inside mymeTypes have extname which im supporting 
  //every single file type you need to have a definitive known, known value,that what type of object it is.

  //"application/octate-stream" :: genetic file type check docs for more info

// next read file  name  fs 

// for high level operation close to the system chNCES OF ERROR , 

 //req has head and body  
        // head :: metadata kind of info eg,. statusCode, is it html, json file etc in head ,
        //body has content part it same for req and res ,eg.,access your cookies, uh, JSON token, session tokens.

